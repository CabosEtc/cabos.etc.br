// script.js

// Espera o DOM carregar completamente antes de executar qualquer código JS
document.addEventListener('DOMContentLoaded', function() {
    console.log("DOM completamente carregado e analisado");
});